<h2 class="etc-pdn-vert">Filial</h2>

<table class="table">
  <thead>
    <tr>
      <th scope="col">Nome</th>
      <th scope="col">CNPJ</th>
      <th scope="col">Endereço</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= @subsidiary.name %></td>
      <td><%= @subsidiary.cnpj %> </td>
      <td><%= @subsidiary.address %></td>
    </tr>
  </tbody>
</table>

<div class="col-sm-5 etc-pdn-top">
    <%= link_to 'Editar', edit_subsidiary_path(@subsidiary), class:"btn btn btn-warning" %>
    <%= link_to 'Apagar', subsidiary_path(@subsidiary), method: :delete, class: "btn btn-danger", data: { confirm: 'Quer apagar esta filial?' } %>
    <%= link_to 'Voltar', subsidiaries_path, class:"btn btn-outline-dark" %>
</div>